[toolchain]
# Bumped from 1.80 → 1.88 because fastembed 5.x → ort 2.0.0-rc.11 requires
# rustc 1.88 (edition2024 stabilized in 1.85; several ort + icu crates MSRV
# climbed to 1.88). Any stable ≥ 1.88 works.
channel = "1.88"
components = ["rustfmt", "clippy"]
